MONGO_URI=mongodb://mongo:27017/ecommerce-users
JWT_SECRET=jwt_secret
PORT=5000
FRONTEND_URL=http://localhost:5173

# Brevo API (recommended — uses HTTPS port 443, works on Railway)
BREVO_API_KEY=
SMTP_FROM=no-reply@nuevavida1327.com
SENDER_NAME=Nueva Vida

EMAIL_VERIFICATION_SECRET=jwt_email_verification_secret
EMAIL_VERIFICATION_EXPIRES_MINUTES=15
EMAIL_SEND_TIMEOUT_MS=15000

# Fallback SMTP (only if BREVO_API_KEY is not set)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=